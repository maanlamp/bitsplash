"use strict";
const measure = (context, taget) => { };
const render = (context, target) => {
    if (typeof target === "string") {
        return renderString(context, target);
    }
};
const renderString = (context, string) => { };
//# sourceMappingURL=box.js.map